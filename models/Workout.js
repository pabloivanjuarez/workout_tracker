const {
  Schema
} = require("mongoose")

const mongoose = require("mongoose");
// read into act 14
Const Schema = mongoose.Schema;

const WorkoutSchema = new Schema({
  day: {
    type: Date
  },
  Exercies: {
    type
  }
})